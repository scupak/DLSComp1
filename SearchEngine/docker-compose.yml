version:  "3.9"

services:
  searchapi: 
    build: 
      context:  .
      dockerfile:  "SearchAPI/Dockerfile"
    ports:
      - 9000:80
    volumes:
      - C:/data:/data
  loadbalancer: 
    build: 
      context:  .
      dockerfile:  "LoadBalancer/Dockerfile"
  websearch:
    build: 
      context:  .
      dockerfile:  "WebSearch/Dockerfile"
    ports:
      - 9001:80

volumes:
  data: